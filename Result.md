# 「あなたが繰り返すミスのクセ診断」診断結果ページ設計書

## 1. スコアリング・判定ロジック

### 基本スコア集計
6タイプそれぞれに対応する3つの質問の回答（-2〜+2点）を合計し、タイプ別基本スコア（-6〜+6点）を算出する。

- **かなり当てはまる**: +2点
- **やや当てはまる**: +1点
- **どちらとも言えない**: 0点
- **あまり当てはまらない**: -1点
- **全く当てはまらない**: -2点

### 補正の適用
- **Q19（忙しい日のクセ強化）**: 回答スコアが+1以上（「かなり」「やや」）の場合、最高スコアタイプの基本スコアに `+1` を加算する。
- **Q20（バイアス自覚度）**: 回答スコアが+1以上の場合、結果画面に「自覚度コメント」を追加表示するフラグをONにする。

### 主タイプ・副タイプおよび複合型の判定ルール
集計後のスコア（補正適用後）に基づき、以下の手順で最終タイプを判定する。

1. **スコア順ソート**:
   タイプ別スコアの高い順にソートし、最も高いスコアを $S_1$（主タイプ）、2番目に高いスコアを $S_2$（副タイプ）とする。
2. **複合型（ハイブリッド型）判定**:
   $S_1$ と $S_2$ の差が **1点以内**（$S_1 - S_2 \le 1$）の場合、**「複合型」**と判定する。
   - 表示例：`ペースセッター型 × 強制型`
   - スコア差が2点以上の場合は、単一の `主タイプ` を結果タイプとする。
3. **比率（％）表示の計算**:
   薬剤師の多面的な傾向を伝えるため、主タイプと副タイプのスコア比率を表示する。
   比率の算出には、負のスコアを排除するために各タイプスコア（-6〜+6）に `+6` を加算して正の値（0〜12）に変換した値（$S'_1, S'_2$）を用いる。
   - 計算式：
     - 主タイプ比率（％） = $\frac{S'_1}{S'_1 + S'_2} \times 100$ （小数点以下四捨五入）
     - 副タイプ比率（％） = $100 - \text{主タイプ比率}$
   - ※ 両方の変換後のスコア合計が0の場合（共に-6点の場合）は、一律50%/50%とする。
   - ※ この比率を表示することで、「主タイプ70% / 副タイプ30%」といった直感的で現実に近いバランスが可視化される。

---

## 2. 結果画面の構成（表示順序：ミス先行型）

ユーザーが「自分の痛いところ（ミス）」を直視し、そこからタイプ解説、解決策へと流れることで、行動変容への納得感を最大化する順序に再編成する。

### 画面遷移フロー
```
最後の質問（Q20）に回答
↓
ローディングアニメーション（1.5秒程度）
「あなたの無意識のクセを分析中...」
↓
結果画面へフェードイン
```

### 結果画面のセクション構成（上から順に表示）

#### ① ⚠️ あなたが最も起こしやすいミス（最上部に配置）
診断結果が「あなたをラベル貼りする性格診断」ではないことを示すため、最初に「ミスのクセ」を突きつける。
- **表示内容**:
  - 主タイプ（複合型の場合は両タイプ）の「起こしやすいミス3選」を大きくカードで表示。
  - 例（ペースセッター型の場合）：
    ```text
    ⚠️ あなたが最も起こしやすいミス
    
    ・説明過多（一方的に話し続ける）
    ・確認不足（患者の理解度を確かめない）
    ・情報過多（一度に多くの情報を与えすぎる）
    ```
- **デザイン**: 警告色（薄い赤または黄色背景）のカードで、視覚的なインパクトを与える。

#### ② 🔥 あなたのタイプ（および比率）
ミスを突きつけられた直後に、その原因となる「自分のタイプ名」を明示する。
- **表示内容**:
  - **通常判定時**: 主タイプ名 ＋ 一言ラベル ＋ キャッチコピー
  - **複合判定時**: 「主タイプ名 × 副タイプ名」のハイブリッド表示
  - **比率表示**: `主タイプ割合%` と `副タイプ割合%`（例：ペースセッター 70% / コーチ 30%）
  - 例（複合型の場合）：
    ```text
    あなたのバイアスタイプ
    🔥 ペースセッター型  ×  ⚡ 強制型
    （説明しすぎ 70%  /  正解を急ぎすぎ 30%）
    
    「説明しているだけで、相手に伝わっていないかもしれません」
    ```
- **デザイン**: タイプのテーマカラー（主タイプのカラー）をベースにしたグラデーションヘッダー。

#### ③ 💡 理由（あなたの傾向・ズレの説明）
なぜそのミスが起きてしまうのか、行動の「ズレ」を納得感のある文章で解説する。
- **表示内容**:
  - Type.mdの「ズレ（行動ベース）」を表示。
  - 複合型の場合は、主タイプと副タイプのズレの説明を自然に並べる。

#### ④ 📊 あなたのバイアス傾向マップ（グラフセクション）
直感的な理解と見栄えの両立を図るため、**「棒グラフ」と「レーダーチャート」を両方表示**する。
- **棒グラフ（理解用・優先表示）**:
  薬剤師にとって最も理解しやすい数値比較用。
  ```text
  ペースセッター ██████████ 80 (点数換算)
  強制型         ██████ 50
  民主型         ████ 30
  ```
  ※ Canvas等を使わず、シンプルなCSS（`width` アニメーション）でスマートに実装可能。
- **レーダーチャート（見栄え用）**:
  全体のバランス、偏り度合いを視覚的に楽しむためのもの。Canvas APIまたはSVGで6角形を描画。

#### ⑤ ✅ 明日からできる行動変容（OK行動 ＋ 実践セリフ）
診断を「気づき」から「行動」に落とし込むための具体的なアクションとフレーズ。
- **OK行動**: Type.mdの「修正行動（1つだけ）」を表示。
- **実践セリフ**: Type.mdの「セリフ」を表示。
- **デザイン**:
  - セリフは大きな吹き出しデザインで表示。
  - 「クリップボードにコピー」ボタンを配置（スマホでタップしてメモや電子カルテの下書き等に貼れるようにする）。

#### ⑥ 📌 サブ傾向の補足（単一判定の場合のみ表示）
複合判定にならなかった場合でも、2番目に高かった傾向について補助的なアドバイスを行う。
- 例：「サブ傾向として『コーチ型』の要素も持っています。相手を見守るあまり、必要な介入が遅れないように注意しましょう。」

#### ⑦ 💡 バイアス自覚度コメント
Q20の判定がONの場合にのみ、表示されるメッセージ。
- 例：「あなたは自分のクセを自覚しにくい傾向があります。定期的に周囲（同僚や指導スタッフ）から『私の服薬指導、どうだった？』とフィードバックをもらうのがおすすめです。」

#### ⑧ 📝 振り返りメモ ＆ 今日から試す行動
今日得た気づきと、明日から試す具体的な行動をその場で選んで保存できるエリア。
- **今日から試す行動（チェックリスト）**:
  - 本タイプに応じた「OK行動」が最初からチェックボックスとして表示される。
  - ユーザーが自分で自由に行動を追加できる入力欄と追加ボタンを配置。
  - 例：
    - `[ ] 1つ説明したら必ず理解確認を入れる`（初期値）
    - `[ ] 患者が話し出すまで3秒待つ`（ユーザー自身が追加した行動）
- **振り返りメモ**:
  - テキスト入力欄（「今日の気づきをメモ」）。
- **保存仕様**:
  - `localStorage` を使用し、スマホのローカル環境にのみ保存される（プライバシーの安全性を明記する）。

#### ⑨ 💾 結果の保存・印刷・共有（新・共有導線）
薬剤師が自分の恥ずかしいミスをSNSに投稿しにくい心理に配慮し、「学びとチーム共有」に焦点を当てた導線をメインとする。
- **結果PDF保存 / 印刷用レイアウト**:
  - `window.print()` を起動するボタン。印刷用にヘッダーや不要な操作ボタンを非表示にし、A4用紙1枚にきれいに収まるスタイルシート（CSS `@media print`）を適用する。
  - 印刷したものは、**「院内勉強会用の自己分析シート」**としてそのまま活用できるように設計。
- **結果画像として保存**:
  - Canvas API を用いて、結果画面の「ミス3選」「タイプ名・比率」「グラフ（棒・レーダー）」を統合した美しい診断結果画像を生成し、画像（PNG）としてダウンロードできるボタン。
- **院内勉強会・指導での活用法**:
  - 「この結果をどう使うか」の短いコラムを表示。（例：「指導薬剤師と共有する」「勉強会のアイスブレイクに使う」など）
- **SNSで宣言（補助導線）**:
  - 恥ずかしいタイプ名を晒すのではなく、「私のミスのクセ対策宣言」として投稿できるようにする。
  - テキスト例：「『薬剤師のミスのクセ診断』を実行。明日から【1つ説明したら必ず理解確認を入れる】を徹底します！ #薬剤師のミスのクセ診断 [URL]」
- **「もう一度診断する」ボタン**。

---

## 3. 保存・印刷用レイアウトの設計（CSS `@media print`）

印刷またはPDF保存時に、以下のスタイルを自動適用する。
- 画面上の「回答ボタン」「保存ボタン」「もう一度診断する」などの操作系UIを完全に非表示にする（`display: none`）。
- 振り返りメモ欄の入力済みテキストや、チェックボックスの選択状態がそのままテキストとして美しく紙面に反映されるようにする。
- ページの背景色を白、文字色を黒にし、プリンターのインク消費を抑えつつ視認性を高める。

---

## 4. データ構造とロジック（JS実装イメージ）

```javascript
// 6タイプ詳細データ
const typeDetails = {
  pacesetter: {
    name: "ペースセッター型",
    label: "説明しすぎ・引っ張りすぎ型",
    color: "#F97316",
    misses: [
      "説明過多（一方的に話し続ける）",
      "確認不足（患者の理解度を確かめない）",
      "情報過多（一度に多くの情報を与えすぎる）"
    ],
    bias: "正しく伝えようとするあまり、患者の理解や反応を待たずに説明を進めてしまう",
    okAction: "1つ説明したら必ず理解確認を入れる",
    serifu: "「ここまでで、わかりにくいところはありませんか？」",
    catchcopy: "「説明しているだけで、伝わっていないかもしれません」"
  },
  // 他の5タイプも同様に記述
};

// 判定ロジック関数
function analyzeResult(answers) {
  // answersは 0〜19 のインデックスを持つ長さ20の配列 (値: -2 〜 +2)
  
  // 各タイプのスコア初期化
  const scores = {
    pacesetter: answers[0] + answers[1] + answers[2],
    coach: answers[3] + answers[4] + answers[5],
    authoritative: answers[6] + answers[7] + answers[8],  // 強制
    affiliative: answers[9] + answers[10] + answers[11],  // 関係重視
    visionary: answers[12] + answers[13] + answers[14],  // 先見
    democratic: answers[15] + answers[16] + answers[17],   // 民主
  };
  
  // 補正Q19の適用
  const isQ19Active = answers[18] >= 1;
  let maxScoreType = Object.keys(scores).reduce((a, b) => scores[a] > scores[b] ? a : b);
  if (isQ19Active) {
    scores[maxScoreType] += 1;
  }
  
  // 補正Q20の適用（自覚度フラグ）
  const isAwarenessLow = answers[19] >= 1;
  
  // スコア順にソート
  const sortedTypes = Object.keys(scores).sort((a, b) => scores[b] - scores[a]);
  const primaryType = sortedTypes[0];
  const secondaryType = sortedTypes[1];
  
  const primaryScore = scores[primaryType];
  const secondaryScore = scores[secondaryType];
  
  // 複合型判定：スコア差が 1 以内
  const isHybrid = (primaryScore - secondaryScore) <= 1;
  
  // 比率計算（スコアに+6して正の値に変換）
  const pScoreNorm = primaryScore + 6;
  const sScoreNorm = secondaryScore + 6;
  const totalNorm = pScoreNorm + sScoreNorm;
  
  let primaryRatio = 50;
  let secondaryRatio = 50;
  if (totalNorm > 0) {
    primaryRatio = Math.round((pScoreNorm / totalNorm) * 100);
    secondaryRatio = 100 - primaryRatio;
  }
  
  return {
    primaryType,
    secondaryType,
    isHybrid,
    primaryRatio,
    secondaryRatio,
    scores,
    isAwarenessLow
  };
}
```

---

## 5. 今後の対応タスク（readme.mdとの同期）

- [x] readme.md のセクション10（スコアリング）を直接計算および複合・比率判定に対応させる。
- [x] readme.md のセクション17・18の開発進捗とステップを最新化する。
